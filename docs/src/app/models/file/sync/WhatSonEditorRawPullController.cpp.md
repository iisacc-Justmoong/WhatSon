# `src/app/models/file/sync/WhatSonEditorRawPullController.cpp`

`WhatSonEditorRawPullController` orders pull requests through caller-provided callbacks.

The current QML editor shell does not bind this controller to active note document state.
