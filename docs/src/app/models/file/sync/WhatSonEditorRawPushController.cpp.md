# `src/app/models/file/sync/WhatSonEditorRawPushController.cpp`

`WhatSonEditorRawPushController` orders RAW push triggers and payload replacement.

It is no longer wired to the current contents editor because the active document session path was deleted.
