# `src/app/models/file/sync/WhatSonEditorRawPushController.hpp`

`WhatSonEditorRawPushController` declares RAW push ordering and stale payload replacement helpers.

The active editor document owner that previously supplied payloads was deleted.
