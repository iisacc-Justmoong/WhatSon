# `src/app/models/editor/component/ResourceImageFrame.h`

`ResourceImageFrame` declares image-resource frame projection helpers.

The helper does not own active editor document state and must not resolve editor-session file paths.
