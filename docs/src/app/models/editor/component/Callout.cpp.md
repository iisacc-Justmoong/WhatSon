# `src/app/models/editor/component/Callout.cpp`

The callout component helper renders and reasons about canonical callout source snippets.

It is no longer connected to the active contents editor because the document session and native key-command paths were deleted.
