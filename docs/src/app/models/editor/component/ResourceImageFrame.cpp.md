# `src/app/models/editor/component/ResourceImageFrame.cpp`

The implementation builds image-resource frame markup from explicit resource descriptor inputs.

It remains a standalone helper and is not wired into the current contents editor document model.
