# `src/app/models/panel/NoteActiveStateTracker.hpp`

`NoteActiveStateTracker` tracks active note identity and backing storage paths for panel coordination.

It does not expose an editor document session path. The current contents editor does not mount active note body files.
