# `src/app/models/calendar/DayCalendarController.hpp`

## Role
`DayCalendarController` exposes day entries and time slots.

## Interface Alignment
- Calendar board dependency is now `ICalendarBoardStore`.
