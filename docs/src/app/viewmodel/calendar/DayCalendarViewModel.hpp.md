# `src/app/viewmodel/calendar/DayCalendarViewModel.hpp`

## Role
`DayCalendarViewModel` exposes day entries and time slots.

## Interface Alignment
- Calendar board dependency is now `ICalendarBoardStore`.
