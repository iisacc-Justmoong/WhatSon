# `src/app/viewmodel/calendar/YearCalendarViewModel.hpp`

## Role
`YearCalendarViewModel` exposes a twelve-month year projection.

## Interface Alignment
- Calendar board injection now depends on `ICalendarBoardStore`.
