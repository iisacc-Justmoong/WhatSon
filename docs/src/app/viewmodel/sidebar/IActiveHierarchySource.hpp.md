# `src/app/viewmodel/sidebar/IActiveHierarchySource.hpp`

## Role
`IActiveHierarchySource` exposes the active hierarchy index and its change signal.

## Notes
- Startup deferred-loading code uses this minimal interface instead of the concrete sidebar viewmodel.
