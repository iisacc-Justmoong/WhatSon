# `src/app/viewmodel/detailPanel/DetailNoteHeaderSelectionSourceViewModel.cpp`

## Implementation Notes
- `setSessionStore(...)` now accepts the session interface.
- Selection synchronization and persistence logic remain unchanged.
