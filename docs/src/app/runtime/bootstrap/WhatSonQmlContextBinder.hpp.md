# `src/app/runtime/bootstrap/WhatSonQmlContextBinder.hpp`

`WorkspaceContextObjects` lists QObjects exported to the QML workspace context.

The deleted document-model objects are intentionally absent: no active editor document session, no editor paste bridge, and no native editor input command filter are exposed to QML.
