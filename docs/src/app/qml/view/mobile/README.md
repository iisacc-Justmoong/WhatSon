# Mobile QML

The mobile QML namespace hosts compact workspace scaffolds and routed pages.

Current mobile editor routing uses `ContentViewLayout.qml` with the same deleted document-model boundary as desktop: no active note document session, no editor paste bridge, and no native editor input command filter.
